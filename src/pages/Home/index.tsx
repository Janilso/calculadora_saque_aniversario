import { Typography } from '@mui/material';

function Home() {
    return (
        <div>
            <Typography
                variant="body2"
                sx={{
                    color: 'appColors.primary',
                }}
            >
                HOme Page
            </Typography>
        </div>
    );
}

export default Home;
